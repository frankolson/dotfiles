# Custom scripts
PATH=$PATH:$HOME/bin
export EDITOR='vim'

# nvm stuff
export NVM_DIR=~/.nvm
source $(brew --prefix nvm)/nvm.sh

# rbenv stuff
if which rbenv > /dev/null; then
  eval "$(rbenv init -)"
fi
